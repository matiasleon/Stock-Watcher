package main

import "fmt"

func main() {

	/* interpreter := interpretermodel.New()

	// create bot
	/stockBot, err := bot.Create(interpreter)
	if err != nil {
		log.Panic(err)
	}

	stockBot.Listen()
	*/
	fmt.Sprintf("Hello")
}
